{
  "$comment": "OpenFin manifest for the AG-Grid Customization Platform demo. Run with `npm run dev:openfin` from the repo root — that boots the Vite dev server and launches OpenFin pointed at this manifest.",

  "licenseKey": "openfin-demo",
  "runtime": {
    "version": "stable",
    "arguments": "--v=1"
  },

  "startup_app": {
    "uuid": "grid-customizer-demo",
    "name": "grid-customizer-demo",
    "url": "http://localhost:5190/",
    "applicationIcon": "http://localhost:5190/favicon.ico",

    "$comment-layout": "1440×900 matches the viewport the e2e tests + screenshot tooling use; resizable so multi-grid dashboards can expand.",
    "autoShow": true,
    "defaultWidth": 1440,
    "defaultHeight": 900,
    "defaultCentered": true,
    "resizable": true,
    "maximizable": true,
    "saveWindowState": true,

    "$comment-chrome": "frame=true keeps the OS title bar (minimize/close/etc). alwaysOnTop=false — popouts pin themselves via `openFinWindowOpener({ alwaysOnTop: true })`, not the root window.",
    "frame": true,
    "alwaysOnTop": false,
    "nonPersistent": false,

    "$comment-perf": "preloading chromium-affinity so popouts from `fin.Window.create` share the same renderer process — avoids multi-process IPC for the cross-window React tree described in PopoutPortal.",
    "experimental": {
      "popupOnVerticalScroll": false
    }
  },

  "shortcut": {
    "company": "Grid Customizer",
    "description": "AG-Grid Customization Platform demo (OpenFin)",
    "icon": "http://localhost:5190/favicon.ico",
    "name": "Grid Customizer Demo",
    "target": ["desktop", "start-menu"]
  },

  "$comment-devtools": "Enable --remote-debugging-port=9090 via runtime.arguments for devtools from Chrome at localhost:9090. Kept off by default; uncomment if you're debugging OpenFin-specific issues.",
  "devtools_port": 9090
}
